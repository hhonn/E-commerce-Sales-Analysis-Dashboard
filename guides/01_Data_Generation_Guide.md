# คู่มือการสร้างข้อมูล (Data Generation Guide)

## 📋 ภาพรวม

เอกสารนี้อธิบายขั้นตอนและหลักการในการสร้างข้อมูลการขาย E-commerce จำลอง สำหรับใช้ในโปรเจกต์วิเคราะห์ข้อมูล

---

## 🎯 วัตถุประสงค์

1. สร้างข้อมูลการขายที่มีความสมจริง (Realistic Mock Data)
2. มีข้อมูลครบถ้วนตามที่กำหนด 1,000 records
3. ครอบคลุมช่วงเวลา 1 ปีย้อนหลัง
4. สามารถนำไปวิเคราะห์ได้จริงด้วย Excel และ Power BI

---

## 🛠️ เครื่องมือและ Library ที่ใช้

### 1. **Python 3.8+**
ภาษาโปรแกรมหลักที่ใช้ในการสร้างข้อมูล

### 2. **Pandas (version 2.0+)**
- **จุดประสงค์**: จัดการและประมวลผลข้อมูลในรูปแบบ DataFrame
- **การใช้งาน**: สร้างตาราง, จัดการคอลัมน์, เรียงลำดับข้อมูล, Export เป็น CSV

### 3. **NumPy (version 1.24+)**
- **จุดประสงค์**: คำนวณทางคณิตศาสตร์และสร้างตัวเลขสุ่มที่มีการกระจายตัวตามสถิติ
- **การใช้งาน**: 
  - `np.random.beta()` - สร้างวันที่ที่มีน้ำหนักเอียงไปทางปัจจุบัน
  - `np.random.gamma()` - สร้างจำนวนสินค้าที่มีการกระจายตัวแบบเบ้ขวา

### 4. **Faker (version 20.0+)**
- **จุดประสงค์**: สร้างข้อมูลปลอมที่ดูสมจริง
- **การใช้งาน**: สร้างชื่อลูกค้าภาษาไทย (`Faker('th_TH')`)

---

## 📊 โครงสร้างข้อมูล (Data Schema)

| Field | Type | Description | Example |
|-------|------|-------------|---------|
| **OrderID** | String | รหัสคำสั่งซื้อ (Unique) | ORD00001 |
| **OrderDate** | Date | วันที่สั่งซื้อ (YYYY-MM-DD) | 2024-11-15 |
| **CustomerID** | String | รหัสลูกค้า (ซ้ำได้ = ลูกค้าซื้อหลายครั้ง) | CUST0042 |
| **CustomerName** | String | ชื่อลูกค้า | สมชาย ใจดี |
| **Region** | String | ภูมิภาค | North, South, East, West, Central |
| **ProductID** | String | รหัสสินค้า | PROD015 |
| **ProductName** | String | ชื่อสินค้า | Smartphone Pro Max |
| **Category** | String | หมวดหมู่สินค้า | Electronics, Clothing, Home Goods, Books |
| **Quantity** | Integer | จำนวนสินค้าที่ซื้อ | 1-10 |
| **UnitPrice** | Float | ราคาต่อหน่วย (บาท) | 15999.00 |
| **TotalPrice** | Float | ราคารวม (Quantity × UnitPrice) | 31998.00 |

---

## 🔬 Logic การสร้างข้อมูล

### 1. **OrderID**
```python
order_id = f"ORD{i:05d}"
```
- สร้างเป็น Sequential ID เริ่มจาก ORD00001 ถึง ORD01000
- Format: ORD + เลข 5 หัก (Zero-padded)

### 2. **OrderDate (วันที่สั่งซื้อ)**
```python
days_diff = (end_date - start_date).days
random_days = int(np.random.beta(2, 5) * days_diff)
order_date = start_date + timedelta(days=random_days)
```
- ใช้ **Beta Distribution (α=2, β=5)** เพื่อให้วันที่เอียงไปทางปัจจุบัน
- จำลองการเติบโตของธุรกิจ (ยอดขายเพิ่มขึ้นเมื่อเวลาผ่านไป)
- ช่วงเวลา: วันนี้ ย้อนหลัง 365 วัน

### 3. **CustomerID & CustomerName**
```python
customer_id = f"CUST{random.randint(1, 300):04d}"
customer_name = fake.name()
```
- สร้างลูกค้า 300 คน (CUST0001-CUST0300)
- ลูกค้าคนเดียวกันสามารถซื้อหลายครั้งได้ (เพื่อจำลอง Repeat Customer)
- ใช้ `Faker('th_TH')` สร้างชื่อภาษาไทยที่ดูสมจริง

### 4. **Region (ภูมิภาค)**
```python
region_weights = [0.15, 0.15, 0.15, 0.15, 0.40]
region = random.choices(REGIONS, weights=region_weights)
```
- ใช้ **Weighted Random** ให้ Central มีน้ำหนัก 40% (จำลองตลาดหลัก)
- ภูมิภาคอื่นๆ มีน้ำหนัก 15% เท่าๆ กัน

### 5. **Category & ProductName**
```python
CATEGORIES = {
    'Electronics': [10 สินค้า],
    'Clothing': [10 สินค้า],
    'Home Goods': [10 สินค้า],
    'Books': [10 สินค้า]
}
```
- มี 4 หมวดหมู่หลัก
- แต่ละหมวดหมู่มี 10 สินค้า รวม 40 สินค้าทั้งหมด
- สุ่มแบบ Uniform (โอกาสเท่ากันทุกสินค้า)

### 6. **Quantity (จำนวน)**
```python
quantity = int(np.random.gamma(2, 1)) + 1
quantity = min(quantity, 10)
```
- ใช้ **Gamma Distribution (k=2, θ=1)** เพื่อให้การกระจายตัวเบ้ขวา
- ส่วนใหญ่จะซื้อ 1-3 ชิ้น, บางครั้งซื้อมากถึง 10 ชิ้น
- สมจริงกับพฤติกรรมการซื้อจริง

### 7. **UnitPrice (ราคาต่อหน่วย)**
```python
PRICE_RANGES = {
    'Electronics': (500, 35000),
    'Clothing': (200, 3500),
    'Home Goods': (150, 8000),
    'Books': (150, 1200)
}
unit_price = round(random.uniform(min_price, max_price), 2)
```
- แต่ละหมวดหมู่มีช่วงราคาที่เหมาะสม
- ใช้ Uniform Distribution สุ่มราคาในช่วง
- ปัดเศษเป็น 2 ตำแหน่ง (สตางค์)

### 8. **TotalPrice (ราคารวม)**
```python
total_price = round(quantity * unit_price, 2)
```
- คำนวณจาก Quantity × UnitPrice
- ปัดเศษเป็น 2 ตำแหน่ง

---

## 🚀 ขั้นตอนการใช้งาน

### ขั้นที่ 1: ติดตั้ง Dependencies

เปิด Command Prompt หรือ PowerShell แล้วรันคำสั่ง:

```powershell
cd c:\Project_Data\data
pip install -r requirements.txt
```

### ขั้นที่ 2: รันสคริปต์สร้างข้อมูล

```powershell
python generate_data.py
```

### ขั้นที่ 3: ตรวจสอบไฟล์ผลลัพธ์

ระบบจะสร้างไฟล์ `sales_data.csv` ในโฟลเดอร์เดียวกัน

---

## 📈 ตัวอย่างผลลัพธ์

### Console Output:
```
======================================================================
🚀 เริ่มสร้างข้อมูลการขาย E-commerce
======================================================================
🔄 กำลังสร้างข้อมูล 1000 records...
✓ สร้างข้อมูลแล้ว 100/1000 records
✓ สร้างข้อมูลแล้ว 200/1000 records
...
✓ สร้างข้อมูลแล้ว 1000/1000 records

📊 สรุปข้อมูลที่สร้าง
======================================================================

📈 จำนวนข้อมูลทั้งหมด: 1,000 records

📅 ช่วงเวลา:
   - วันที่เริ่มต้น: 2023-11-04
   - วันที่สิ้นสุด: 2024-11-02

💰 ยอดขายรวม: ฿3,458,967.80
   - ยอดขายเฉลี่ยต่อออเดอร์: ฿3,458.97
   - ยอดขายสูงสุด: ฿121,450.00
   - ยอดขายต่ำสุด: ฿150.25

✅ บันทึกข้อมูลเรียบร้อยแล้ว: sales_data.csv
```

### ตัวอย่างข้อมูลใน CSV:

| OrderID | OrderDate | CustomerID | CustomerName | Region | ProductID | ProductName | Category | Quantity | UnitPrice | TotalPrice |
|---------|-----------|------------|--------------|--------|-----------|-------------|----------|----------|-----------|------------|
| ORD00001 | 2023-11-10 | CUST0125 | สมชาย ใจดี | Central | PROD001 | Smartphone Pro Max | Electronics | 2 | 15999.00 | 31998.00 |
| ORD00002 | 2023-11-12 | CUST0087 | สมหญิง ดีมาก | North | PROD025 | Running Shoes | Clothing | 1 | 1899.50 | 1899.50 |

---

## 🔍 การตรวจสอบคุณภาพข้อมูล

### 1. **Completeness (ความสมบูรณ์)**
- ✅ ทุก record มีข้อมูลครบทุกคอลัมน์
- ✅ ไม่มีค่า NULL หรือค่าว่าง

### 2. **Consistency (ความสอดคล้อง)**
- ✅ TotalPrice = Quantity × UnitPrice (ตรวจสอบได้)
- ✅ OrderDate อยู่ในช่วง 365 วัน
- ✅ ราคาอยู่ในช่วงที่กำหนดของแต่ละหมวดหมู่

### 3. **Validity (ความถูกต้อง)**
- ✅ OrderID ไม่ซ้ำ (Unique)
- ✅ Region อยู่ใน 5 ภูมิภาคที่กำหนด
- ✅ Category อยู่ใน 4 หมวดหมู่ที่กำหนด
- ✅ Quantity เป็นจำนวนเต็มบวก (1-10)

### 4. **Realism (ความสมจริง)**
- ✅ ใช้ Beta Distribution ทำให้ยอดขายเติบโตตามเวลา
- ✅ ใช้ Gamma Distribution ทำให้จำนวนสินค้าสมจริง
- ✅ มีลูกค้าซื้อซ้ำ (Repeat Customer)
- ✅ Central มียอดขายมากกว่า (ตลาดหลัก)

---

## 📝 สรุปแนวคิดหลัก

1. **Reproducibility**: ใช้ `seed=42` ทำให้ได้ผลลัพธ์เดิมทุกครั้ง
2. **Statistical Realism**: ใช้ Distribution ทางสถิติแทนการสุ่มแบบธรรมดา
3. **Business Logic**: ออกแบบข้อมูลให้สอดคล้องกับธุรกิจจริง
4. **Scalability**: สามารถปรับจำนวน records ได้ง่าย (แค่เปลี่ยน parameter)

---

## 🎓 ประโยชน์ต่อการเรียนรู้

- เข้าใจการใช้ Python Libraries สำหรับ Data Engineering
- เรียนรู้การออกแบบข้อมูลที่มีคุณภาพ
- เข้าใจ Statistical Distribution และการนำไปใช้จริง
- สามารถนำไปประยุกต์ใช้กับโปรเจกต์อื่นได้

---

## ➡️ ขั้นตอนถัดไป

หลังจากสร้างข้อมูลเรียบร้อยแล้ว ให้ดำเนินการดังนี้:

1. ✅ **ตรวจสอบไฟล์** `sales_data.csv` ว่าสร้างสำเร็จ
2. 📊 **วิเคราะห์ด้วย Excel** - ดูคู่มือใน `02_Excel_Analysis_Guide.md`
3. 📈 **สร้าง Dashboard ด้วย Power BI** - ดูคู่มือใน `03_PowerBI_Dashboard_Guide.md`
4. 🌐 **พัฒนา Web Application** - ดูโฟลเดอร์ `web-app/`

---

**หมายเหตุ**: ข้อมูลที่สร้างขึ้นเป็นข้อมูลจำลองเพื่อการศึกษาเท่านั้น ไม่ได้มาจากธุรกิจจริง
