# 🏗️ สถาปัตยกรรมโปรเจกต์ (Project Architecture)

## 📊 ภาพรวมระบบ (System Overview)

```
┌─────────────────────────────────────────────────────────────────┐
│                   E-COMMERCE SALES ANALYSIS                     │
│                    Full-Stack Dashboard                          │
└─────────────────────────────────────────────────────────────────┘

     ┌──────────────┐
     │  ส่วนที่ 1   │
     │ Data Layer   │
     └──────┬───────┘
            │
            ▼
  ┌─────────────────────┐
  │  Python Script      │
  │  generate_data.py   │
  │  (Faker, Pandas)    │
  └─────────┬───────────┘
            │
            ▼
  ┌─────────────────────┐
  │  sales_data.csv     │
  │  (1,000 records)    │
  └─────────┬───────────┘
            │
            ├─────────────────────┬─────────────────────┐
            │                     │                     │
            ▼                     ▼                     ▼
     ┌──────────┐         ┌──────────┐         ┌──────────┐
     │ ส่วนที่ 2 │         │ ส่วนที่ 3 │         │ ส่วนที่ 4 │
     │  Excel   │         │Power BI  │         │ Web App  │
     └──────────┘         └──────────┘         └──────────┘
            │                     │                     │
            ▼                     ▼                     ▼
  ┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
  │ Analysis        │   │ Dashboard       │   │ React UI        │
  │ • Descriptive   │   │ • KPI Cards     │   │ • Login         │
  │ • Regression    │   │ • Line Chart    │   │ • CRUD          │
  │ • Moving Avg    │   │ • Bar Chart     │   │ • Search/Filter │
  └─────────────────┘   │ • Pie Chart     │   │ • Export CSV    │
                        │ • Table         │   └─────────────────┘
                        └─────────┬───────┘
                                  │
                                  ▼
                        ┌─────────────────┐
                        │ Power BI Service│
                        │ (Publish to Web)│
                        └─────────┬───────┘
                                  │
                                  ▼
                        ┌─────────────────┐
                        │  Embed URL      │
                        │  (Public Link)  │
                        └─────────┬───────┘
                                  │
                                  └───────────┐
                                              │
                                              ▼
                                    ┌──────────────────┐
                                    │   ส่วนที่ 5      │
                                    │  Deployment      │
                                    └─────────┬────────┘
                                              │
                                              ▼
                                    ┌──────────────────┐
                                    │  Netlify/Vercel  │
                                    │  (Cloud Hosting) │
                                    └─────────┬────────┘
                                              │
                                              ▼
                                    ┌──────────────────┐
                                    │   Public URL     │
                                    │   🌐 Internet    │
                                    └──────────────────┘
```

---

## 🔄 Data Flow (การไหลของข้อมูล)

```
1. DATA GENERATION
   ┌─────────────────────────────────────┐
   │ Python Script (generate_data.py)    │
   │ • Faker: Generate fake names        │
   │ • NumPy: Statistical distributions  │
   │ • Pandas: Data manipulation         │
   └─────────────┬───────────────────────┘
                 │
                 ▼
   ┌─────────────────────────────────────┐
   │ sales_data.csv                      │
   │ 11 columns × 1,000 rows             │
   │ • OrderID, Date, Customer, etc.     │
   └─────────────────────────────────────┘

2. DATA ANALYSIS (3 Paths)
   ┌─────────────────────────────────────┐
   │           Excel Analysis            │
   │ ┌─────────────────────────────────┐ │
   │ │ A. Descriptive Statistics       │ │
   │ │    • Mean, Median, SD           │ │
   │ │    • Min, Max, Range            │ │
   │ └─────────────────────────────────┘ │
   │ ┌─────────────────────────────────┐ │
   │ │ B. Regression Analysis          │ │
   │ │    • Y = TotalPrice             │ │
   │ │    • X = Quantity               │ │
   │ │    • R² = 0.21                  │ │
   │ └─────────────────────────────────┘ │
   │ ┌─────────────────────────────────┐ │
   │ │ C. Moving Average (7-Day)       │ │
   │ │    • Smoothing trend            │ │
   │ │    • Identify seasonality       │ │
   │ └─────────────────────────────────┘ │
   └─────────────────────────────────────┘

3. DATA VISUALIZATION
   ┌─────────────────────────────────────┐
   │        Power BI Dashboard           │
   │ ┌───────┬───────┬───────┐           │
   │ │ KPI 1 │ KPI 2 │ KPI 3 │  ← Cards │
   │ └───────┴───────┴───────┘           │
   │ ┌─────────────────────────┐         │
   │ │   Line Chart (Monthly)  │         │
   │ └─────────────────────────┘         │
   │ ┌───────────┬─────────────┐         │
   │ │ Bar Chart │  Pie Chart  │         │
   │ └───────────┴─────────────┘         │
   │ ┌─────────────────────────┐         │
   │ │   Table (Top 10)        │         │
   │ └─────────────────────────┘         │
   └─────────────────────────────────────┘

4. WEB APPLICATION (React)
   ┌─────────────────────────────────────┐
   │  Components Architecture            │
   │                                     │
   │  ┌──────────────────────┐          │
   │  │      App.js          │  ← Main  │
   │  │   (State Manager)    │          │
   │  └──────────┬───────────┘          │
   │             │                       │
   │    ┌────────┼────────┐             │
   │    │        │        │             │
   │    ▼        ▼        ▼             │
   │  Login  Dashboard  Data Mgmt       │
   │  Page     Page      Page           │
   │                                     │
   │  Features:                          │
   │  • Add/Edit/Delete (CRUD)          │
   │  • Search/Filter/Sort              │
   │  • Export CSV                       │
   │  • Power BI Embed                   │
   └─────────────────────────────────────┘

5. DEPLOYMENT
   ┌─────────────────────────────────────┐
   │     Build Process                   │
   │  npm run build                      │
   │       ↓                             │
   │  build/ folder                      │
   │       ↓                             │
   │  Upload to Netlify                  │
   │       ↓                             │
   │  https://xxx.netlify.app            │
   └─────────────────────────────────────┘
```

---

## 🎨 Web App Architecture (รายละเอียด)

```
web-app/
│
├── public/                     # Static files
│   ├── index.html             # HTML template
│   └── _redirects             # Netlify routing
│
├── src/
│   │
│   ├── index.js               # Entry point
│   │      │
│   │      └──> React.render(<App />)
│   │
│   ├── App.js                 # Main component
│   │      │
│   │      ├──> React Router (BrowserRouter)
│   │      ├──> State Management (useState)
│   │      │    • isAuthenticated
│   │      │    • salesData (array)
│   │      │
│   │      └──> Routes:
│   │           • /login → LoginPage
│   │           • /dashboard → DashboardPage
│   │           • /data-management → DataManagementPage
│   │           • /analysis-summary → AnalysisSummaryPage
│   │
│   ├── components/
│   │   └── Layout.js          # Navigation wrapper
│   │        │
│   │        ├──> AppBar (Top)
│   │        ├──> Drawer (Sidebar)
│   │        └──> Outlet (Content area)
│   │
│   └── pages/
│       │
│       ├── LoginPage.js       # ฟังก์ชันที่ 2
│       │    • Mock authentication
│       │    • Username/Password form
│       │
│       ├── DashboardPage.js   # ฟังก์ชันที่ 1
│       │    • Power BI iframe embed
│       │    • Responsive container
│       │
│       ├── DataManagementPage.js  # ฟังก์ชันที่ 3-9, 11
│       │    │
│       │    ├──> DataGrid (MUI X)
│       │    │    • Display table
│       │    │    • Built-in sort
│       │    │
│       │    ├──> Toolbar
│       │    │    • Search box (ฟังก์ชันที่ 5)
│       │    │    • Region filter (ฟังก์ชันที่ 6)
│       │    │    • Add button (ฟังก์ชันที่ 3)
│       │    │    • Export CSV (ฟังก์ชันที่ 11)
│       │    │
│       │    ├──> Dialogs
│       │    │    • Add Dialog (ฟังก์ชันที่ 3)
│       │    │    • Edit Dialog (ฟังก์ชันที่ 8)
│       │    │    • Delete Dialog (ฟังก์ชันที่ 9)
│       │    │
│       │    └──> State
│       │         • searchQuery
│       │         • filterRegion
│       │         • formData
│       │
│       └── AnalysisSummaryPage.js  # ฟังก์ชันที่ 10
            • Display Excel results
            • Formatted tables
            • Insights & recommendations
```

---

## 🔧 Technology Stack (รายละเอียด)

```
┌─────────────────────────────────────────────────────────┐
│                    TECHNOLOGY STACK                     │
└─────────────────────────────────────────────────────────┘

1. DATA LAYER
   ┌──────────────────────────────────┐
   │ Python 3.8+                      │
   │ • pandas 2.0+    → Data frames   │
   │ • numpy 1.24+    → Calculations  │
   │ • faker 20.0+    → Mock data     │
   └──────────────────────────────────┘

2. ANALYSIS LAYER
   ┌──────────────────────────────────┐
   │ Microsoft Excel 2016+            │
   │ • Analysis ToolPak               │
   │   - Descriptive Statistics       │
   │   - Regression                   │
   │   - Moving Average               │
   └──────────────────────────────────┘

3. VISUALIZATION LAYER
   ┌──────────────────────────────────┐
   │ Power BI Desktop                 │
   │ • DAX (Data Analysis Expr.)      │
   │ • Power Query                    │
   │ • Power BI Service (Cloud)       │
   └──────────────────────────────────┘

4. FRONTEND LAYER
   ┌──────────────────────────────────┐
   │ React 18.2                       │
   │ • React Router 6                 │
   │ • Material-UI (MUI) 5            │
   │ • MUI X Data Grid                │
   │ • PapaParse (CSV)                │
   │ • Emotion (CSS-in-JS)            │
   └──────────────────────────────────┘

5. DEPLOYMENT LAYER
   ┌──────────────────────────────────┐
   │ Netlify / Vercel                 │
   │ • Git-based deployment           │
   │ • Continuous Deployment          │
   │ • HTTPS automatic                │
   │ • Custom domain support          │
   └──────────────────────────────────┘
```

---

## 📱 UI Component Hierarchy (MUI)

```
App (ThemeProvider + CssBaseline)
│
├── Router (BrowserRouter)
│   │
│   ├── Route: /login
│   │   └── LoginPage
│   │       ├── Container
│   │       ├── Paper
│   │       │   ├── Avatar (LockIcon)
│   │       │   ├── Typography (Title)
│   │       │   ├── TextField (Username)
│   │       │   ├── TextField (Password)
│   │       │   └── Button (Submit)
│   │       └── Alert (Error message)
│   │
│   └── Route: / (Protected)
│       └── Layout
│           ├── AppBar (Top navigation)
│           ├── Drawer (Sidebar)
│           │   └── List
│           │       ├── ListItem (Dashboard)
│           │       ├── ListItem (Data Mgmt)
│           │       ├── ListItem (Analysis)
│           │       └── ListItem (Logout)
│           │
│           └── Outlet (Main content)
│               │
│               ├── DashboardPage
│               │   ├── Typography (Title)
│               │   ├── Alert (Instructions)
│               │   └── Paper
│               │       └── iframe (Power BI)
│               │
│               ├── DataManagementPage
│               │   ├── Typography (Title)
│               │   ├── Paper (Toolbar)
│               │   │   ├── TextField (Search)
│               │   │   ├── TextField (Filter)
│               │   │   ├── Button (Add)
│               │   │   └── Button (Export)
│               │   │
│               │   ├── Paper (Table)
│               │   │   └── DataGrid
│               │   │       └── Columns
│               │   │           └── Actions
│               │   │               ├── IconButton (Edit)
│               │   │               └── IconButton (Delete)
│               │   │
│               │   └── Dialogs
│               │       ├── Dialog (Add)
│               │       ├── Dialog (Edit)
│               │       └── Dialog (Delete)
│               │
│               └── AnalysisSummaryPage
│                   ├── Typography (Title)
│                   ├── Paper (Intro)
│                   ├── Paper (Descriptive Stats)
│                   │   └── Grid
│                   │       ├── Card (TotalPrice)
│                   │       └── Card (Quantity)
│                   ├── Paper (Regression)
│                   │   ├── Table
│                   │   └── Card (Equation)
│                   └── Paper (Moving Average)
│                       ├── Card (Purpose)
│                       └── Card (Interpretation)
```

---

## 🔐 Data Security & Privacy

```
┌────────────────────────────────────────────┐
│         SECURITY CONSIDERATIONS            │
└────────────────────────────────────────────┘

1. MOCK DATA
   ✅ All data is FAKE (generated by Faker)
   ✅ No real customer information
   ✅ Safe for educational use

2. AUTHENTICATION
   ⚠️  Mock login (no real security)
   ⚠️  Username/Password not validated
   ℹ️  For demonstration only

3. DATA STORAGE
   ✅ React State (temporary)
   ⚠️  Not persistent (lost on refresh)
   ℹ️  No backend database

4. POWER BI EMBED
   ⚠️  Public embed link (anyone can view)
   ℹ️  Suitable for public dashboards only
   ℹ️  Don't use for sensitive data

5. DEPLOYMENT
   ✅ HTTPS automatic (Netlify)
   ✅ Environment variables supported
   ⚠️  Frontend only (no backend secrets)
```

---

## 🚀 Performance Optimization

```
┌────────────────────────────────────────────┐
│      PERFORMANCE BEST PRACTICES            │
└────────────────────────────────────────────┘

1. REACT OPTIMIZATION
   ✅ useMemo() for filtered data
   ✅ Debounced search input
   ✅ Lazy loading components
   ✅ Code splitting (routes)

2. DATA GRID
   ✅ Virtual scrolling (DataGrid)
   ✅ Pagination (10/25/50 rows)
   ✅ Column sorting (client-side)

3. BUILD OPTIMIZATION
   ✅ Production build (npm run build)
   ✅ Minification automatic
   ✅ Tree shaking
   ✅ Asset optimization

4. POWER BI
   ⚠️  Embed may be slow (external)
   ℹ️  Use lightweight visuals
   ℹ️  Limit data refresh rate
```

---

## 📊 Data Model (Entity Relationship)

```
┌──────────────────────────────────────┐
│         SALES DATA MODEL             │
└──────────────────────────────────────┘

Order (Fact Table)
┌─────────────────────────────────────┐
│ OrderID      (PK) String            │
│ OrderDate         Date              │
│ CustomerID   (FK) String            │
│ ProductID    (FK) String            │
│ Quantity          Integer           │
│ UnitPrice         Decimal           │
│ TotalPrice        Decimal (calc.)   │
└─────────────────────────────────────┘
        │               │
        │               │
        ▼               ▼
┌──────────────┐  ┌──────────────┐
│  Customer    │  │   Product    │
│  (Dimension) │  │  (Dimension) │
├──────────────┤  ├──────────────┤
│ CustomerID   │  │ ProductID    │
│ CustomerName │  │ ProductName  │
│ Region       │  │ Category     │
└──────────────┘  └──────────────┘

Relationships:
• Order.CustomerID → Customer.CustomerID (Many-to-One)
• Order.ProductID → Product.ProductID (Many-to-One)

Calculated Fields:
• TotalPrice = Quantity × UnitPrice
• Total Sales = SUM(TotalPrice)
• Total Orders = COUNT(OrderID)
• Avg Sale = Total Sales / Total Orders
```

---

## 🎓 Learning Path (แผนที่การเรียนรู้)

```
START
  │
  ▼
┌─────────────────────────────────┐
│ 1. Python Basics                │
│    • Variables, Loops            │
│    • Lists, Dictionaries        │
│    • Functions                   │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ 2. Data with Python             │
│    • Pandas DataFrame            │
│    • NumPy Arrays                │
│    • Faker Library               │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ 3. Excel Analysis               │
│    • Pivot Tables                │
│    • Analysis ToolPak            │
│    • Statistical Functions       │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ 4. Power BI                     │
│    • Import Data                 │
│    • DAX Measures                │
│    • Visualizations              │
│    • Publishing                  │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ 5. Web Development (React)      │
│    • HTML, CSS, JavaScript       │
│    • React Components            │
│    • React Hooks                 │
│    • Material-UI                 │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ 6. Deployment                   │
│    • Git & GitHub                │
│    • Build Process               │
│    • Cloud Hosting               │
└─────────────┬───────────────────┘
              │
              ▼
           FINISH
      (Full-Stack Developer!)
```

---

## 🎯 Project Milestones (Timeline)

```
Week 1: Data Generation & Analysis
├── Day 1-2: Python script + CSV
└── Day 3-5: Excel analysis (3 methods)

Week 2: Dashboard Creation
├── Day 1-3: Power BI visuals
└── Day 4-5: Publish & get embed link

Week 3: Web Development
├── Day 1-2: Setup React + routing
├── Day 3-4: Build all 4 pages
└── Day 5: Test all 11 functions

Week 4: Polish & Deploy
├── Day 1-2: UI improvements
├── Day 3: Deploy to Netlify
├── Day 4: Documentation
└── Day 5: Final testing + Report
```

---

**สถาปัตยกรรมนี้ออกแบบมาเพื่อความเรียนรู้และความเข้าใจในระบบ Full-Stack!** 🎓✨
